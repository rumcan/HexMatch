/**
 * Story mode is HIDDEN for now: no Story Mode door on the start screen, no
 * campaign lines or story Continue on the main menu, and `?chapter=` playtest
 * links are ignored. The campaign code stays in place — flip this to bring it
 * back.
 */
export const STORY_MODE_ENABLED = false;
