{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hexmatch-atlas-manifest",
  "title": "HexMatch isometric atlas manifest (E1)",
  "description": "One shared manifest for all scale variants of the sprite atlas (K1 Kenney edition). `anchor` is the pixel inside the sprite that lands exactly on the screen position of the footprint's diamond CENTRE — for ground/standing sprites the measured widest row of the base diamond, for vehicles the bottom-centre. Computed by the packer; never hand-authored.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "images",
    "tileW",
    "tileH",
    "sprites"
  ],
  "properties": {
    "tileW": {
      "type": "integer",
      "minimum": 1
    },
    "tileH": {
      "type": "integer",
      "minimum": 1
    },
    "meta": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "source": {
          "type": "string"
        },
        "license": {
          "type": "string"
        },
        "credits": {
          "type": "string"
        }
      }
    },
    "sprites": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/sprite"
      },
      "propertyNames": {
        "pattern": "^[a-z0-9_]+$"
      }
    },
    "images": {
      "type": "object",
      "description": "Atlas image per zoom level (0.5/1/2). Coordinates & anchors are at 1x.",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "$defs": {
    "sprite": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "x",
        "y",
        "w",
        "h",
        "footprint",
        "anchor"
      ],
      "properties": {
        "kind": {
          "description": "Anchor class: ground (flat block, widest-row anchor), standing (building, base-diamond anchor) or vehicle (bottom-centre anchor; intentionally smaller than its tile).",
          "type": "string",
          "enum": ["ground", "standing", "vehicle"]
        },
        "x": {
          "type": "integer",
          "minimum": 0
        },
        "y": {
          "type": "integer",
          "minimum": 0
        },
        "w": {
          "type": "integer",
          "minimum": 1
        },
        "h": {
          "type": "integer",
          "minimum": 1
        },
        "frames": {
          "type": "integer",
          "minimum": 1
        },
        "frameMs": {
          "type": "integer",
          "minimum": 1
        },
        "footprint": {
          "type": "array",
          "prefixItems": [
            {
              "type": "integer",
              "minimum": 1
            },
            {
              "type": "integer",
              "minimum": 1
            }
          ],
          "minItems": 2,
          "maxItems": 2
        },
        "anchor": {
          "type": "array",
          "prefixItems": [
            {
              "type": "integer",
              "minimum": 0
            },
            {
              "type": "integer",
              "minimum": 0
            }
          ],
          "minItems": 2,
          "maxItems": 2
        },
        "slices": {
          "description": "E4 Tier-3 escape hatch: optional per-tile strips for sprites that cannot be depth-sorted as a whole. A slice is an axis-aligned strip with its own sub-rect.",
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "x",
              "y",
              "w",
              "h"
            ],
            "properties": {
              "x": {
                "type": "integer",
                "minimum": 0
              },
              "y": {
                "type": "integer",
                "minimum": 0
              },
              "w": {
                "type": "integer",
                "minimum": 1
              },
              "h": {
                "type": "integer",
                "minimum": 1
              }
            }
          }
        },
        "parts": {
          "description": "MB1 multi-storey stack: present only on a composite 'standing' building sprite. The sprite's x/y/w/h are its UNION bounding box (x/y unused); each part names a packed layer sprite (a storey/base/roof tile, tinted at pack time) drawn at a (dx,dy) offset from the box's top-left, bottom-to-top order. A composite is ONE draw/order/pick object on its footprint.",
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "sprite",
              "dx",
              "dy"
            ],
            "properties": {
              "sprite": {
                "type": "string",
                "pattern": "^[a-z0-9_]+$"
              },
              "dx": {
                "type": "integer"
              },
              "dy": {
                "type": "integer"
              }
            }
          }
        },
        "variants": {
          "description": "MB2 per-instance variants: optional ordered pick-set on the canonical sprite (variants[0] === this sprite's own name). Each entry names another manifest sprite (usually another composite) the renderer may draw instead, one per instance by a stable tile seed, so repeated buildings aren't identical. Every entry must share the footprint and be player-tinted.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$"
          }
        }
      }
    }
  }
}