# G9 — Committed derived rail art is stale: the G7 atlas gate is red

**Status:** FIXED — 2026-09-05, on `arena/01a0717e-hexmatch`. See
[Resolution](#resolution-2026-09-05) at the bottom.
**Filed:** OPEN — found by the Priority-1 play-test, 2026-09-05.
**Severity:** MEDIUM. `main` does not pass its own test suite.
**Area:** `src/iso/kenny/derived/`, `tools/make-derived-art.mjs`.

---

## The claim in the backlog is wrong

`docs/HexMatch-open-backlog.md` states:

> Verified: typecheck clean, **333 unit tests pass**, `slice-atlas` reproduces
> the atlas byte-identically (G7 gate green) […] The rail-pixel test the PR
> flagged as failing now **passes** (12/12) — no longer an issue.

Measured on `arena/01a07072-hexmatch` @ `3c0c64b` (branch of `main`):

```
npm run typecheck   → clean
npm test            → Test Files 1 failed | 18 passed (19)
                      Tests 1 failed | 332 passed (333)
```

The failure is **not** a flake:

```
FAIL tests/unit/iso-atlas-pixels.test.ts > K2 road masks …
  > rail masks carry rails on their set arms (derived art honours the table)
AssertionError: expected 125 to be +0
  ❯ tests/unit/iso-atlas-pixels.test.ts:128:47
    128| expect(await railAt("rail_0101", 99, 48)).toBe(0);  // SE: no rail
```

## Root cause

Two committed PNGs do not match what `tools/make-derived-art.mjs` produces
today. They were generated by an older revision of the script and never
regenerated:

| file | committed | regenerated |
|------|-----------|-------------|
| `src/iso/kenny/derived/rail_0101.png` | 12 791 B | 2 272 B |
| `src/iso/kenny/derived/rail_1010.png` | 12 662 B | 2 284 B |

The stale `rail_0101` carries dark rail pixels at the **SE** arm midpoint
(125 dark pixels in the test's 21×11 window), which is an unset arm for mask
`0101` (NE+SW) — exactly the 90°-off class of bug the test exists to catch.

## Fix (verified)

```bash
node tools/make-derived-art.mjs   # rewrites rail_*.png, crossing, highlight
npm run slice-atlas               # repack (verified: atlas is byte-identical,
                                  #          assets/iso-atlas/ shows no diff)
npm test                          # → 12/12 pixel tests pass, 333/333 green
```

I ran all three while investigating and confirmed the suite goes green and the
packed atlas does **not** change (`git status assets/iso-atlas/` empty) — the
staleness is confined to the two source PNGs. I then reverted the working tree
so this PR stays on its own ticket, per the backlog's one-ticket-per-PR rule
and its "don't touch the art pipeline" instruction.

## Acceptance

1. `node tools/make-derived-art.mjs` produces **no** diff against the committed
   `src/iso/kenny/derived/*.png` (the derived art is a build output; it must be
   in sync with its generator).
2. `npm test` → 333/333, including `K2 road masks > rail masks carry rails on
   their set arms`.
3. `npm run slice-atlas` → `git status assets/iso-atlas/` clean.
4. Optional but recommended: a test or CI step that regenerates the derived art
   and fails when the output differs from what is committed, so this class of
   drift cannot reappear silently.

## Out of scope

- Do NOT hand-edit any PNG, and do not hand-edit `tools/iso-atlas.cells.json`.
- Do NOT re-do MB1 stacking or the Kenney mapping.

---

## Resolution (2026-09-05)

Fixed exactly as prescribed, plus the recommended drift gate (acceptance 4).

**What changed**

| file | change |
|------|--------|
| `src/iso/kenny/derived/rail_0101.png` | regenerated — 12 791 B → **2 272 B** |
| `src/iso/kenny/derived/rail_1010.png` | regenerated — 12 662 B → **2 284 B** |
| `tools/make-derived-art.mjs` | new `--check` (regenerate into a temp dir, compare byte-for-byte with what is committed, exit 1 on drift) and `--out DIR`; the default write path is unchanged |
| `tests/unit/iso-derived-art.test.ts` | new — runs `--check` from `npm test`, so the drift is caught locally and not only by the CI atlas gate |

No PNG was hand-edited and `tools/iso-atlas.cells.json` was not touched.

**Acceptance**

1. ✅ `node tools/make-derived-art.mjs` produces no diff against the committed
   `src/iso/kenny/derived/*.png` — and `--check` says
   `derived art in sync with its generator (19 PNGs)`, exit 0.
2. ✅ `npm test` → **352/352** (was 332/333), including
   `K2 road masks > rail masks carry rails on their set arms`.
3. ✅ `npm run slice-atlas` → `git status assets/iso-atlas/` empty. The packed
   atlas was already correct; the staleness was confined to the two source
   PNGs, exactly as this ticket predicted.
4. ✅ Added — `tools/make-derived-art.mjs --check` +
   `tests/unit/iso-derived-art.test.ts`. Verified the gate actually bites:
   with the two stale PNGs restored from `HEAD` it exits 1 and names them
   (`rail_0101.png: committed 12791 B ≠ regenerated 2272 B`). The existing CI
   step (`make-derived-art` + `slice-atlas` + `git diff --exit-code`) is
   unchanged and still covers the packed atlas too.

The false "333 unit tests pass" / "rail-pixel test passes" claims at the top of
`docs/HexMatch-open-backlog.md` are corrected there in the same PR.
