# ART-1950S / TICKET-B4 — dead-art audit (2026-09-11)

Generated by `node tools/art-audit.mjs` against the built game (`http://localhost:4173/hexmatch/?seed=79`).
**Nothing was deleted.** Candidates need sign-off; see the method below.

## Session coverage

- factory placed at [4,22] (rival auto-seated), setup finished
- 1 depots, 0 plants, 2 road drags
- 900 fast-forward ticks: aiTick + econTick + quarry + trucks (2 trucks running)
- full-map middle-drag camera sweep at 0.5× / 1× / 2×
- **77 sprite names observed blitted** (`__iso.drawnSprites`)

## Accounted-for (NOT candidates)

- 172 by construction: 16 road_/dirt_/dirt_road_ bitmasks ×3,
  depot_<cargo> ×6, TOWN_HOUSE_VARIANTS ×43, terrain ×3, truck_goods ×4,
  node_mark, highlight ×2
- 64 written literally in src/tools/tests/server/rundot
- 77 observed at runtime (subset overlap with the above)

## Deletion candidates (74 of 239) — NEED SIGN-OFF

### ore (5)
- `ore_mine_t0`
- `ore_mine_t2`
- `ore_mine_t3`
- `ore_mine_t5`
- `ore_mine_t6`

### farm (6)
- `farm_t33`
- `farm_t34`
- `farm_t35`
- `farm_t36`
- `farm_t37`
- `farm_t38`

### forest (1)
- `forest_t16`

### oil (1)
- `oil_rig_t29`

### gold (16)
- `gold_mine_t72`
- `gold_mine_t73`
- `gold_mine_t74`
- `gold_mine_t75`
- `gold_mine_t76`
- `gold_mine_t77`
- `gold_mine_t78`
- `gold_mine_t79`
- `gold_mine_t80`
- `gold_mine_t81`
- `gold_mine_t82`
- `gold_mine_t83`
- `gold_mine_t84`
- `gold_mine_t85`
- `gold_mine_t86`
- `gold_mine_t87`

### quarry (16)
- `quarry_t72`
- `quarry_t73`
- `quarry_t74`
- `quarry_t75`
- `quarry_t76`
- `quarry_t77`
- `quarry_t78`
- `quarry_t79`
- `quarry_t80`
- `quarry_t81`
- `quarry_t82`
- `quarry_t83`
- `quarry_t84`
- `quarry_t85`
- `quarry_t86`
- `quarry_t87`

### factory (12)
- `factory_blue`
- `factory_red`
- `factory_purple`
- `factory_green`
- `factory_mt_blue_0`
- `factory_mt_blue_1`
- `factory_mt_blue_2`
- `factory_mt_blue_3`
- `factory_mt_red_0`
- `factory_mt_red_1`
- `factory_mt_red_2`
- `factory_mt_red_3`

### depot (4)
- `depot_blue`
- `depot_red`
- `depot_purple`
- `depot_green`

### town (3)
- `town_house_a`
- `town_house_b`
- `town_house_c`

### spare (10)
- `spare_farm_arctic_tropic`
- `spare_fruit_plantation`
- `spare_saw_mill`
- `spare_paper_mill`
- `spare_print_works`
- `spare_power_station`
- `spare_oil_refinery`
- `spare_bungalows`
- `spare_stadium_american_football`
- `spare_stadium_soccer`

## Method (why these are only CANDIDATES)

The audit unions the runtime-observed set with every by-construction family
and every literal name. The remainder are candidates, not deletions:

1. Some families are situational (protest art, MP flows) — a single seed and
   a 900-tick session does not exercise every branch.
2. `spare_*` cells exist as packing overflow from the TTD house sheets;
   `factory_mt_*` is documented legacy ("the game no longer emits them").
3. Removing a name is a two-stage change (manifest + `npm run slice-atlas`,
   then the source art) and moves `iso-manifest`/`iso-atlas-pixels`/
   `iso-pp12-assets`/`iso-golden` assertions — update deliberately.

Before deleting anything: re-run this audit on more seeds, add a deliberate
playtest of the branches above, and get explicit sign-off on this list.

## Safe cleanups done in this pass

- `assets/iso-atlas/contact-sheet.png` (712K) and `footprint-check.png`
  (251K): confirmed imported by nothing (only regenerated by
  `tools/slice-atlas.mjs` / `tools/footprint-check.mjs`). Untracked from git
  and ignored — the tools still write them locally for review, but they no
  longer read as shipped art. CI's G7 `git diff --exit-code assets/iso-atlas/`
  stays green (untracked + ignored files are outside `git diff`).
- `assets/new-atlas/`: does not exist in this tree (already removed at some
  point after the migration doc was written).
- `assets/ground/grass.png`: the migration doc's "2.6 MB" is stale — the file
  is 235,875 bytes, already palette-quantised (PSNR ∞ vs a 64-colour
  re-encode) and zlib-optimal. No load-time win available here; nothing done.

## Explicitly NOT done (needs sign-off)

- The 74 candidates above are untouched.
- Root-level `01a07ce9-*.patch` and `building-layers.patch` look like stray
  artifacts (265K combined) — the migration doc says ask before removing.
